apiVersion: apps/v1
kind: Deployment
metadata:
  name: commandservice
  namespace: kubeorion
spec:
  replicas: 1
  selector:
    matchLabels: { app: commandservice }
  template:
    metadata:
      labels: { app: commandservice }
    spec:
      # If you create a docker-registry secret (Option B below), uncomment:
      # imagePullSecrets:
      #   - name: acr-auth
      containers:
        - name: commandservice
          image: acrorionbusdemo.azurecr.io/commandservice:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8080 }]
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
          envFrom:
            - secretRef: { name: command-secrets }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: commandservice
  namespace: kubeorion
spec:
  type: LoadBalancer
  selector: { app: commandservice }
  ports:
    - name: http
      port: 80
      targetPort: 8080
